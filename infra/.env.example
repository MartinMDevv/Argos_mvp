# Plantilla de variables de entorno para la base de datos de Argos.
# Copiar este archivo a ".env" (mismo directorio) y poner una contraseña real.
# El .env real NO sube a git (está en .gitignore).

POSTGRES_USER=argos
POSTGRES_PASSWORD=cambia_esta_contraseña
POSTGRES_DB=argos
POSTGRES_PORT=5432
